#first project.

#What did I do in this project
#Downloaded Git for Windows
#Created a directory to work out of
#used "git init" to initialize git in my directory
#created this readme.md file and added the first line
#used "git add ." to add all files to the main branch(in this case it is just the readme.md file)
#used 'git commit -m "comment"' to commit the readme.md file to the master branch

#created a repository on github named 'sample' so i can save this project to the cloud
#used 'git remote add origin ....' to link the github repository to my local working directory
#used 'git push -u origin master' to push my local working directory main branch to the github repository

#created calculatory.py and added it using 'git add .'
#committed the changes to the master branch using 'git commit -m "comment"'
#pushed my changes to the github repository using 'git push'

#Started documenting my steps
#used 'git add .' to add the changes to the master branch
#used 'git commit -m "comment"' to commit my changes
#used 'git push' to push them to my repository

#learning about branches
#used 'git checkout -b new-branch' to create a new branch named new-branch
#created branch.txt
#did git add and git commit
#did 'git push origin new-branch' to push the new branch up to github
#created a pull request for the new branch
#clicked 'merge pull request' to merge it into the master branch
#went back to the terminal and did 'git pull origin master' to sync up my local working directory and the github repository  
